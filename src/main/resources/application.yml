app:
  job:
    scheduler:
      delay: 5000
      interval: 5000

  files:
    directory:
      in: ${user.home}/data/in
      out: ${user.home}/data/out

  filename:
    readonly: .dat
    done: .done.dat

logging:
  level:
    com:
      marlonklc: DEBUG
